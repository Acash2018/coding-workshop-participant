fastapi==0.115.6
mangum==0.19.0
# Pure Python (py3-none-any) - no compiled extension, so no wheel/ABI risk.
PyJWT==2.10.1
# 3.2.3 (the version used by backend/_examples) publishes no cp313 wheel for
# psycopg-binary, and the Lambda runtime is python3.13. 3.2.10 is the first
# release with one.
psycopg[binary]==3.2.13
